{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb", // Using LLDB debugger for macOS
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}/build/${fileBasenameNoExtension}", // Path to the compiled binary
            "args": [], // Optional: Add arguments to pass to the program
            "cwd": "${fileDirname}", // Current working directory
            "preLaunchTask": "build" // Build task before debugging
        }
    ]
}